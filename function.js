const a = 20
const b = 30

function addTwoNumbers(a, b){
    return a + b;
}
console.log(addTwoNumbers(5, 10));

const addup = (a, b) => a + b;
console.log(addup(5, 10));

function minustask (a, b){
    return("minus = ", a - b);
}
minustask(10, 4);

const multiply = (a, b) => console.log("multiply = ", a * b);
multiply(50, 10);

minustask(400, 300) 

function adds(y,z){
    return y + z;
}

const sum = adds(2,3);
console.log("sum = ", sum);

// 